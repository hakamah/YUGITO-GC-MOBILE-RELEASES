name: Temporary Battle V1 GDRE full tool

on:
  push:
    paths:
      - '.github/workflows/temp-battlev1-gdre-full.yml'

permissions:
  contents: read

jobs:
  tool:
    runs-on: ubuntu-latest
    steps:
      - name: Download GDRETools
        run: |
          curl -L -o /tmp/gdre.zip https://github.com/GDRETools/gdsdecomp/releases/download/v2.6.4/GDRE_tools-v2.6.4-linux.zip
          unzip -q /tmp/gdre.zip -d /tmp/gdre
          chmod +x /tmp/gdre/gdre_tools.x86_64
      - name: Upload full tool folder
        uses: actions/upload-artifact@v4
        with:
          name: battlev1-gdre-full
          path: /tmp/gdre
          retention-days: 1
